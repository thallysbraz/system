<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        <small>{{postagem.conteudo}}</small>
        <hr>
        <small>Data de publicação: {{postagem.data}}</small><br>
    </div>
</div>