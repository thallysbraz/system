<div class="jumbotron">
    <h2>Lista de suas disciplinas teste:</h2>
    <hr>

    <table class="table table-striped table-bordered mt-3">
        <thead class="thead-dark">
            <tr>
                <th scope="col">MATRICULA</th>
            </tr>
        </thead>
        <tbody>
            {{#each matricula}}
            <tr>
                <th scope="row">{{mat}}</th>
            </tr>
            {{else}}
            {{/each}}
        </tbody>
    </table>
</div>